DB에서 Text는
별도의 길이 제한을 지정하지 않으며, 훨씬 큰 대용량의 문자열을 저장 할 수 있다.

데이터 크기가 클 경우에 메모리 버퍼가 아닌 별도의 외부 오프 페이지([[Off-page]]) 공간에 저장이 되고, 행에는 포인터만 남기 때문에 추가적인 디스크 읽기가 발생 할 수 있음.

그리고 컬럼에 대해 인덱스 생성이 되지 않고, 별도의 접두사 [Prefix]]길이를 따로 지정을 해줘야 인덱스를 만들 수 있음.

그럼 왜 쓰지?
- 값이 너무 커 row 행의 값이 넘어가려 할 때, 자주 사용한다.

