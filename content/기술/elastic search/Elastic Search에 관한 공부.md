![[Pasted image 20260806164059.png]]ES의 전체적인 아키텍쳐,
content Source에서 ES는 검색에 사용 되기 위해 별도의 재시작이나 상태의 갱신이 필요하지 않는다.
그저 데이터의 색인 작업이 완료되면 바로 동시에 검색 할 수 있다는 것..

ES는 단일 [[노드]]로 시스템을 구성한 후에 시스템의 규모가 늘어나면, 기존 노드 + 새 노드를 실행해서 연결하는 것으로 쉽게 시스템 확장이 가능하다.

데이터는 각 노드에 분산 저장되고 복사본을 유지해서 각종 충돌로부터 [[노드]] 데이터 유실을 방지한다.

NoSQL과 같이 Schema Free를 지원하고, 별도의 사전 매핑 없이 JSON 문서 형식으로 데이터 입력하면 바로 검색 가능한 색인 작업을 수행한다.
RDB에서는 데이터를 테이블 구조로 저장하지만, 아파치 Lucene에서는 데이터를 검색 할 수 있는 구조로 저장하기 위해서 [[색인]] 절차를 거쳐 입력된 텍스트로 토큰을 변환해 역파일 색인 이라는 구조로 저장한다.

Elastic Search와 MongoDB는 모두 JSON문서를 사용하므로 사용 방법이 유사하지만, 서로 다른 목적으로 만들어 졌다. 보통 저장이 중요하면 MongoDB, 검색이 중요하면 Elastic Search를 사용하도록 권장한다.

Elastic Search는 [[Multi Tenancy]] 를 지원하기에, 여러 인덱스를 한꺼번에 묶어 검색 or 처리 가능하다.

[[RestAPI]]를 이용해서 처리한다. nori 한글어 형태소 분석기를 이용해 문장을 토큰화 하여 검색한다.

동해물과 -> 동해+물+과 처럼.

### PostSearchService 의 after commit 이후
Spring Event Listener의 phase를 RDS의 색인 commit 이후로 했기 때문에, ES의 역색인 작업의 실패가 




