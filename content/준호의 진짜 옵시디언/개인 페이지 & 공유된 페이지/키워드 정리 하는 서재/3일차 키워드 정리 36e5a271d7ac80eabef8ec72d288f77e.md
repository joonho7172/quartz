# 3일차 키워드 정리

synchronized

임계 영역(Critical Section)

경쟁 상태(Race Condition)

volatile

메모리 가시성

Lock

(선택. 나중에 수업으로 진행합니다.)ReentrantLock

tryLock()

lockInterruptibly()

(선택) ReadWriteLock

Atomic Types 

CAS(Compare-And-Swap)

(선택) ABA Problem

ThreadLocal

데드락

기아 상태

(선택) 동시성 유틸리티

- CountDownLatch
- CyclicBarrier
- Semaphore
- Phaser
- Exchanger

(선택) 동기화

- StampedLock
- 낙관적 락(Optimistic Lock)
- 비관적 락(Pessimistic Lock)
- 스핀 락

(필수) synchronized 키워드를 메서드에 선언할 때와 블록으로 사용할 때의 차이점을 설명하고, 각각 어떤 객체를 락으로 사용하는지 설명해주세요.

(필수) Race Condition이 무엇인지 설명하고, 이를 해결하기 위한 방법 3가지를 예시와 함께 설명해주세요.

(필수) ReentrantLock이 synchronized보다 유용한 상황 3가지를 들고, 각각의 경우에 대해 코드 예시를 들어 설명해주세요.

volatile 키워드가 가시성 문제는 해결하지만 원자성은 보장하지 못하는 이유를 CPU 캐시와 메인 메모리 관점에서 설명해주세요.

AtomicInteger의 incrementAndGet()이 synchronized 없이도 스레드 안전한 이유를 설명해주세요.

ThreadLocal의 동작 원리를 설명하고, 메모리 누수가 발생할 수 있는 상황과 해결 방법을 설명해주세요.

데드락이 발생하는 4가지 필요조건을 설명하고, 조건을 방지하는 방법을 제시해주세요.

synchronized(this)와 synchronized(lock Object)의 차이점을 설명하고, 별도의 lock 객체를 사용하는 것이 권장되는 이유를 설명해주세요.

volatile boolean flag와 AtomicBoolean의 차이점을 설명하고, 각각 어떤 상황에서 사용해야 하는지 예시를 들어 설명해주세요.

Lock.tryLock()과 Lock.lock()의 차이점을 설명하고, tryLock()이 유용한 실무 시나리오를 2가지 제시해주세요.