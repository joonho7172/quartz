# 34일차 키워드 정리

1.  this가 무엇을 가르키는지는 함수를 어떻게 호출 했는가로 정해진다. 이 점이 어디에서 선언했는가로 정해지는 스코프와의 차이와, 똑같이 생긴 함수를 객체의 메서드로 호출 할 때와 변수에 담아 단독으로 호출할 때의 this의 차이가?
    1. 함수를 어디서 선언했는지 중요한 스코프와는 다르게 this는 호출된 순간 내부의 this를 수행함.
    2. 똑같이 생긴 함수를 객체의 메서드로 호출 할 때는 서로 다른 this, 객체고 변수에 담아 단독으로 호출하면 기본 생성 this가 된다.
2. this 바인딩 방식 네가지의 우선순위와 obj.getName()과 obj.getName.call(ohter)이 서로 다른 결과를 내는 이유를 우선순위로 설명.
    1. new - 명시적 - 암시적 - 기본 의 우선순위를 가진다. obj에서의 getName을 실행하고 obj내에서 this 실행, call이 들어가면 other의 this. 실행
3. 기본 바인딩에서 함수 단독 호출, this가 무엇이 되는지 strict 모드 유무에 따라서 설명, this를 undefined가 되는 것이 어떤 실수를 막아주는지
    1. this가 기본 바인딩에서 단독 호출하면 브라우저의 this인 window가 나오게 된다. 그러나 strict 모드라면 undefined가 나올 것. 이러한 기능이 나중에 런타임 오류를 막아준다. 컴파일에서 막아준다는 뜻.
4. call apply, bind의 공통점과 차이점을 설명, 나중에 호출될 콜백이라면 무엇이 적절한지?
    1. 셋의 공통점은 this를 미리 설정해준다는것임. 명시적으로 선언해준다는 것. 차이점은 bind는 함수를 바로 실행시키지 않고 나중에 콜백에서 실행시킨다는 차이점이 있음.
5. new로 생성자 함수를 호출 할 때 this가 어떻게 바인딩되고 반환값이 어떻게 정해지는지.. 실수로 new를 빠트리고 호출하면 this와 반환값이 각각 어떻게?
    1. 생성자 함수를 호출 할 때this는 새 객체로 바인딩 되고, 특별한 return 이 없으면 this를 반환한다.
    2. 만약 빠트리면 그냥 일반 함수가 호출이 됨.
6. 화살표 함수가 this를 정하는 방식이 일반 함수와 어떻게 다른지, 메서드를 화살표 함수로 작성하면 왜 의도한 객체를 가르키지 못하는지, 반대로 메서드 안의 콜백을 화살표 함수로 작성하면 왜 문제가 해결되는지
    1. 화살표 함수는 this가 내부의 자기 자신 this를 만들지 않고 외부의 this를 참고 해서 만든다, 메서드 안의 콜백을 화살표 함수로 작성하면 자기 자신의 this를 잃어버리지 않고 외부의 this를 가져오기 때문에 해결
7. 어떤 객체의 메서드를 setTimeout 같은 곳에 콜백으로 넘겼더니 내부의 this가 그 객체를 가르키지 않았다. 이 현상이 왜 일어나는지.. 이를 해결하는 방법을 화살표 방법 유무의 방식과 나누어서.
    1. setTImeout 메서드를 사용하면 함수만 빠져나가고 this는 나가지 못한다. 그래서 화살표 방법으로 사용하면 내부의 this를 가르키지 않기 때문에 이러한 문제를 해결 할 수 있다.

클로저

1. 클로저를 함수가 선언될 당시의 환경을 기억한다는 관점에서 정의, 렉시컬 스코프와 어떤 관계인지.
    1. 함수가 선언될 당시의 환경을 기억한다는 것은 그 안에 변수나 메소드들을 기억한다는 것, 그리고 그 범위가 곧 렉시컬 스코프이다.
2. 프로토 타입에 메서드를 하나만 올려 여러 객체가 공유하는 데도 객체마다 다른 결과가 나오는 이유를 this와 연결 지어서 설명.
    1. 프로토 타입에서 메서드를 공유해도 결국 각자 자신만의 this를 가지기 때문에 서로 다른 객체를 가르켜 결과가 다를 수 있다.
3. 외부 함수 실행이 끝났는데도 반한된 함수가 그 지역 변수에 계속 접근 할 수 있는