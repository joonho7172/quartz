# 기술 스택 선정 보고서

## 1. 프로젝트 개요

본 프로젝트는 회원, 게시글, 그룹, 물품 교환 요청, 채팅 등을 제공하는 웹 기반 백엔드 시스템이다. 주요 데이터가 서로 관계를 맺고 있으며, 교환 상태 변경과 같은 작업은 여러 데이터를 하나의 작업 단위로 처리할 필요가 있다.

이에 따라 개발 언어와 [[Framework]]는 다음 기준을 중심으로 선정한다.

- 타입 안정성과 오류 조기 발견
- 동시 요청 처리와 실행 성능
- 메모리 및 자원 관리
- 장기 운영과 장애 분석
- 데이터 무결성과 트랜잭션
- 유지보수성과 생태계

## 2. 개발 언어 선정: Java

### 2.1 언어별 비교

| 기준     | Java                                          | Python                                   | C                  | C#                      |
| ------ | --------------------------------------------- | ---------------------------------------- | ------------------ | ----------------------- |
| 타입 체계  | 정적 타입 언어                                      | 동적 타입 언어                                 | 정적 타입 언어           | 정적 타입 언어                |
| 실행 방식  | 바이트코드를 JVM에서 실행하며 HotSpot [[Jit compiler]] 사용 | CPython은 인터프리터 기반이며 PyPy 등은 JIT 지원       | 네이티브 코드로 컴파일       | CLR의 JIT 또는 AOT 방식으로 실행 |
| 메모리 관리 | Java 힙은 [[Garbarge Collection]]가 관리           | 구현체에 따라 다름. CPython은 참조 카운팅과 순환 참조 GC 사용 | 메모리 관리를 개발자가 직접 처리 | GC가 관리하며 외부 자원은 별도 처리   |
| 주요 강점  | 웹 백엔드, 서버 애플리케이션, 운영 도구                       | 빠른 개발, 데이터·AI 활용                         | 시스템·임베디드·하드웨어 제어   | .NET 기반 웹·서버 애플리케이션     |

### 2.2 타입 안정성과 오류 조기 발견

Java는 정적 타입 언어이므로 변수와 메서드의 타입을 컴파일 시점에 검사한다. 타입이 맞지 않거나 존재하지 않는 메서드를 호출하는 경우 빌드 단계에서 오류가 발생한다.

컴파일 시점에 확인할 수 있는 오류와 실행 중 발생할 수 있는 오류는 다음과 같다.

| 컴파일 시점에 확인할 수 있는 오류 | 실행 중 발생할 수 있는 오류 |
|---|---|
| 타입 불일치 | NullPointerException |
| 문법 오류 | DB 연결 실패 |
| 존재하지 않는 메서드 호출 | 네트워크 오류 |
| 일부 변수 초기화 오류 | 논리 오류와 동시성 오류 |

Java의 정적 타입 검사는 개발 과정에서 발견할 수 있는 오류의 범위를 넓혀 주지만, 런타임 오류와 논리 오류까지 모두 제거하는 것은 아니다.

Python은 기본적으로 실행 중 타입이 결정된다. 다만 mypy, pyright와 같은 정적 검사 도구를 사용하면 일부 타입 오류를 실행 전에 확인할 수 있다.

### 2.3 실행 최적화와 JIT 컴파일

Java의 HotSpot JVM은 실행 중 메서드 호출 횟수와 코드 실행 정보를 수집한다. 자주 실행되는 코드는 JIT 컴파일러가 네이티브 코드로 변환하고, 인라이닝과 분기 최적화 등을 적용한다.

Python에도 PyPy의 JIT나 최근 CPython의 실험적 JIT 기능이 있다. 따라서 Java의 장점은 JIT의 존재 여부보다는 HotSpot의 런타임 프로파일링과 JIT 최적화가 오랫동안 발전해 왔다는 점에 있다.

JIT는 실행 초기에 워밍업 시간이 필요할 수 있으며, 실제 성능은 CPU 연산량, I/O 비중, 메모리 크기, 프레임워크 설정에 따라 달라진다.

### 2.4 메모리 및 자원 관리

C는 일반적으로 개발자가 메모리를 직접 할당하고 해제해야 한다. Java는 더 이상 참조되지 않는 Java 힙 객체를 Garbage Collector가 자동으로 회수한다.

Java GC가 관리하는 대상과 애플리케이션에서 별도로 관리해야 하는 자원은 구분할 필요가 있다.

| 대상 | 관리 방법 |
|---|---|
| Java 힙 객체 | Garbage Collector가 회수 |
| 파일 | close 또는 try-with-resources 사용 |
| 소켓 | 명시적으로 종료 |
| DB 커넥션 | 사용 후 커넥션 풀에 반환 |
| 네이티브 메모리 | 별도 해제 정책 필요 |


### 2.5 장기 운영과 장애 분석

Java는 JVM 상태와 애플리케이션 동작을 분석하기 위한 도구를 제공한다.

| 도구 | 용도 |
|---|---|
| Thread Dump | 스레드의 상태와 스택, 락 대기 상황 확인 |
| Heap Dump | JVM 힙의 객체와 참조 관계 분석 |
| GC 로그 | GC 발생 횟수와 소요 시간, 회수량 확인 |
| JFR | CPU, 메모리, 락, I/O, 예외 등의 JVM 이벤트 기록 |
| jcmd | 스레드 덤프와 JVM 상태 확인 |

Python도 faulthandler, tracemalloc, cProfile과 같은 진단 도구를 제공한다. Java는 JVM 수준의 진단 정보를 JDK 도구로 수집하고 분석할 수 있다는 점에서 서버 운영에 적합하다.

### 2.6 Java 선정 결과

Python은 빠른 개발과 데이터·AI 활용에 강점이 있고, C는 하드웨어 제어와 낮은 수준의 최적화에 강점이 있다. C#은 CLR, JIT, GC를 제공하므로 Java와 비슷한 기술적 장점을 가진다.

본 프로젝트는 장기간 운영되는 웹 백엔드로서 타입 안정성, 동시 요청 처리, 메모리 관리, 장애 분석, 유지보수성을 중요하게 고려한다. Spring Boot와 JVM 생태계를 함께 활용할 수 있다는 점을 고려하여 Java를 개발 언어로 선정한다.

## 3. 객체지향 프로그래밍 ([[OOP]])

객체지향 프로그래밍은 프로그램을 객체 단위로 나누고, 객체 사이의 책임과 상호작용으로 동작을 표현하는 방식이다.

소프트웨어는 계속 변경되므로 객체의 책임과 의존 관계를 적절하게 나누어 변경 사항이 시스템 전체로 퍼지는 것을 줄이는 것이 중요하다.

| 개념 | 의미 | 적용 효과 |
|---|---|---|
| 캡슐화 | 객체 내부 상태를 직접 변경하지 않고 정해진 메서드로 접근 | 잘못된 상태 변경 방지 |
| 추상화 | 사용에 필요한 역할을 드러내고 세부 구현을 숨김 | 사용 코드와 구현 코드 분리 |
| 다형성 | 같은 역할을 여러 구현체가 수행 | 구현체 교체와 확장 |
| 상속 | 기존 타입을 기반으로 하위 타입을 정의 | 타입 간 관계 표현과 코드 재사용 |

상속은 상위 타입과 하위 타입의 관계를 표현하는 데 사용한다. 단순한 코드 재사용이 목적이라면 객체를 조합하는 합성이 더 적합한 경우도 있다.

## 4. Java 버전: 25

Java 25는 장기 운영을 고려할 수 있는 LTS 버전이다. Java 21에서 정식으로 추가된 Virtual Thread와 이후 JVM 개선 사항을 사용할 수 있다.

### 4.1 Virtual Thread

Virtual Thread는 많은 작업이 DB나 외부 API 응답을 기다리는 웹 서버 환경에서 동시 작업 수를 늘리기 위한 기능이다. CPU 연산 자체를 플랫폼 스레드보다 빠르게 처리하는 기능은 아니다.

JDK 21부터 23까지는 synchronized 내부에서 블로킹 작업이 발생할 때 Virtual Thread가 carrier thread에 고정되는 pinning 문제가 있었다. JDK 24에서는 synchronized로 인한 pinning 문제가 개선되었으며, 네이티브 메서드나 Foreign Function 호출에 의한 pinning 가능성은 별도로 확인해야 한다.

### 4.2 Compact Object Headers

Compact Object Headers는 JDK 24에서 실험적으로 도입되었고 JDK 25에서 제품 기능으로 변경되었다. 64비트 환경에서 객체 헤더 크기를 기존 96비트 또는 128비트에서 64비트로 줄여 힙 사용량과 데이터 지역성을 개선할 수 있다.

실제 성능과 메모리 사용량의 변화는 애플리케이션의 객체 사용 패턴에 따라 달라질 수 있으므로 운영 환경 적용 전 부하 테스트를 진행한다.

## 5. Spring Boot

Spring Framework는 Java 애플리케이션 개발을 위한 오픈소스 프레임워크이고, Spring Boot는 Spring 애플리케이션의 설정과 실행을 단순하게 해준다.

Spring Boot를 사용하는 이유는 IoC와 DI를 통해 객체 간 결합도를 낮추고, 웹 MVC, 트랜잭션, 보안, 데이터 접근과 같은 기능을 일관된 방식으로 사용할 수 있기 때문이다.

### 5.1 POJO 기반 구성

POJO는 특정 프레임워크의 클래스를 반드시 상속하거나 특정 인터페이스를 구현해야만 동작하는 객체가 아닌 일반적인 Java 객체를 의미한다.

Spring은 애플리케이션 객체를 일반적인 Java 클래스 형태로 작성할 수 있도록 지원한다. 개발자는 객체의 핵심 책임과 비즈니스 로직에 집중하고, 객체 생성과 연결은 컨테이너에 맡길 수 있다.

### 5.2 DI를 통한 객체 관계 구성

의존성 주입은 객체가 필요한 의존성을 직접 생성하지 않고 외부에서 전달받는 방식이다. Spring 컨테이너는 Bean을 생성하면서 생성자, Setter 또는 필드에 필요한 객체를 주입한다.

| 주입 방식 | 특징 |
|---|---|
| 생성자 주입 | 필수 의존성을 표현하고 final 사용 가능. 객체가 완전히 초기화된 상태로 생성됨 |
| Setter 주입 | 선택적이거나 재설정 가능한 의존성에 사용 |
| 필드 주입 | Reflection으로 주입되며 단위 테스트와 불변 객체 구성에 불리함 |

생성자 주입에서 final을 사용할 수 있는 이유는 생성자에서 값을 할당하기 때문이다. RequiredArgsConstructor는 Lombok이 필요한 필드를 기준으로 생성자를 만들어주는 기능이며, Spring은 생성된 생성자를 사용해 의존성을 주입한다.

### 5.3 IoC, 제어의 역전

Spring을 사용하지 않는다면 객체 생성과 의존 관계 연결을 개발자 코드에서 직접 처리한다.

```java
PaymentService paymentService = new KakaoPaymentService();
OrderService orderService = new OrderService(paymentService);
```

Spring을 사용하면 Spring 컨테이너가 Bean을 생성하고 의존 관계를 연결한다. OrderService는 KakaoPaymentService라는 구체 클래스가 아니라 PaymentService라는 역할에 의존하고, 실제 구현체는 설정에 따라 결정된다.

### 5.4 AOP와 트랜잭션

Spring의 Transactional은 AOP 프록시를 통해 동작한다.

1. 외부 코드가 Spring Bean의 프록시를 호출한다.
2. 프록시가 트랜잭션을 시작한다.
3. 실제 메서드를 실행한다.
4. 정상 종료 시 커밋하고 설정된 예외가 발생하면 롤백한다.

Transactional은 같은 클래스 내부의 메서드 호출에서는 프록시를 거치지 않아 적용되지 않을 수 있다. 또한 메서드 내부에서 새 스레드나 Virtual Thread를 생성해도 기존 트랜잭션이 자동으로 전파되지는 않는다.

## 6. 데이터베이스 선정: MySQL

### 6.1 관계형 데이터베이스를 사용하는 이유

관계형 데이터베이스는 데이터를 테이블 형태로 저장하고, 기본 키와 외래 키 등의 제약 조건으로 테이블 사이의 관계를 관리한다.

회원, 게시글, 그룹, 물품 교환 요청, 채팅은 서로 연관되어 있으며, 교환 상태 변경처럼 여러 데이터를 하나의 작업 단위로 처리해야 하는 경우가 있다. 이러한 데이터 구조와 처리 방식에는 관계형 데이터베이스가 적합하다.

### 6.2 MySQL의 장점

| 항목 | 내용 |
|---|---|
| 데이터 무결성 | 기본 키, 유니크, 외래 키, 체크 제약 조건 사용 |
| 트랜잭션 | InnoDB에서 commit, rollback, 장애 복구 지원 |
| 동시성 | MVCC와 행 단위 잠금 지원 |
| 데이터 조회 | JOIN, 정렬, 집계, 서브쿼리 등 SQL 기능 사용 |
| 운영 생태계 | 다양한 운영 도구와 클라우드 서비스 활용 가능 |

### 6.3 운영 시 고려 사항

관계형 데이터베이스는 명시적인 스키마를 사용하므로 스키마 변경 시 마이그레이션과 기존 데이터와의 호환성 관리가 필요하다.

JOIN의 성능은 JOIN 개수만으로 결정되지 않는다. 데이터 양, 인덱스, 조건절, 실행 계획에 따라 달라진다.

MySQL은 복제, 읽기 전용 Replica, 파티셔닝, 샤딩 등을 이용해 확장할 수 있다. 다만 샤딩 환경에서는 여러 데이터베이스에 걸친 JOIN과 트랜잭션 처리가 복잡해질 수 있다.

## 7. Redis

Redis는 메모리 기반의 자료구조 서버다. 문자열, 리스트, 셋, 해시 등의 자료구조와 TTL, 카운터, 캐시 기능을 제공한다.

Redis는 데이터를 메모리 중심으로 처리하기 때문에 단순한 조회와 갱신이 빠르다. RDB 스냅샷과 AOF 로그 방식으로 데이터를 저장할 수도 있지만, persistence를 사용하지 않거나 장애 직전까지 디스크에 반영되지 않은 데이터는 손실될 수 있다.

### 7.1 위치 인증 토큰

위치 인증 후 그룹 가입 과정에서 발급하는 토큰을 Redis에 저장한다.

- 토큰에 TTL을 설정해 일정 시간이 지나면 자동 만료
- 토큰과 사용자·그룹 정보를 연결
- 다른 그룹에서 사용할 수 없도록 그룹 정보 검증
- 사용이 끝난 토큰은 원자적으로 삭제해 재사용 방지

토큰을 확인한 뒤 삭제하는 과정은 GETDEL이나 Lua Script 등을 사용해 동시에 요청이 들어와도 중복 사용되지 않도록 처리한다.

### 7.2 Refresh Token

Refresh Token은 만료 시간과 회전 정책이 있기 때문에 Redis에 저장할 수 있다. 서버에서 강제 로그아웃이나 토큰 회수를 지원하려면 토큰 또는 세션 정보를 서버 측에 저장한다.

Redis 장애 시 기존 세션을 어떻게 처리할지에 따라 Redis persistence, 복제, 장애 조치 구성을 결정한다. 데이터 손실이 허용되지 않거나 감사 기록이 필요한 데이터는 MySQL과 역할을 구분한다.

### 7.3 캐시

상품 상세나 채팅 목록처럼 조회가 많고 변경 빈도가 상대적으로 낮은 데이터는 Redis에 캐시할 수 있다. 원본 데이터는 MySQL에 저장하고, 데이터 변경 시 캐시를 삭제하거나 갱신한다.

## 8. Kafka

Kafka는 분산 이벤트 스트리밍 플랫폼이다. 메시지 브로커처럼 사용할 수 있으며, 이벤트를 보존하고 재처리할 수 있다는 특징이 있다.

| 개념 | 의미 |
|---|---|
| Event 또는 Record | 전달되는 하나의 데이터 |
| Topic | 이벤트를 저장하는 논리적인 스트림 |
| Partition | Topic을 나눈 순서가 있는 로그 |
| Producer | 이벤트를 Topic에 기록하는 애플리케이션 |
| Consumer | 이벤트를 읽고 처리하는 애플리케이션 |
| Consumer Group | 여러 Consumer가 Partition을 나누어 처리하는 단위 |
| Offset | Consumer가 어디까지 읽었는지 나타내는 위치 |

Producer가 이벤트를 Topic에 기록하면 Kafka는 이를 Partition에 저장한다. 같은 Partition 안에서는 순서가 보장되지만, 여러 Partition 전체의 순서가 항상 보장되는 것은 아니다.

Consumer가 이벤트를 읽었다고 해서 이벤트가 바로 삭제되는 것도 아니다. Topic의 보존 정책에 따라 일정 기간 저장되므로 Offset을 이동해 과거 이벤트를 다시 처리할 수 있다.

### 8.1 교환 상태 변경 처리

교환 상태가 완료되었을 때 교환 완료 이벤트를 발행하고, 여러 Consumer가 후속 작업을 처리한다.

```text
ExchangeCompleted
 ├─ 알림 Consumer: 시스템 메시지 생성
 ├─ 캐시 Consumer: 상품·채팅 목록 캐시 삭제
 └─ 추천 Consumer: 완료 거래를 추천 데이터에 반영
```

DB 상태 변경과 Kafka 발행을 각각 처리하면 DB 변경만 성공하거나 Kafka 발행만 성공하는 상황이 발생할 수 있다. 이를 줄이기 위해 MySQL 트랜잭션 안에 Outbox 이벤트를 저장한 뒤 별도의 Publisher가 Kafka에 발행하는 방식을 사용할 수 있다.

Consumer는 동일한 이벤트를 두 번 이상 받을 수 있다는 전제로 구현하고, 이벤트 ID를 이용해 중복 처리를 방지한다. 실패한 이벤트를 재시도할 수 있도록 재시도 정책도 함께 구성한다.

## 9. 기술별 역할

| 기술    | 역할                                       |
| ----- | ---------------------------------------- |
| MySQL | 회원·게시글·그룹·교환·채팅의 원본 데이터 저장               |
| Redis | 위치 인증 토큰, Refresh Token, 세션, 캐시, TTL 데이터 |
| Kafka | 교환 상태 변경 이벤트와 비동기 후속 처리                  |

현재 서비스의 핵심 데이터는 MySQL에 저장하고, 만료가 필요하거나 빠른 조회가 필요한 데이터는 Redis에서 관리한다. 여러 후속 처리와 이벤트 재처리가 필요한 경우 Kafka를 사용한다.

## 10. 추가 개념

### 10.1 공유 잠금과 배타 잠금

공유 잠금은 여러 트랜잭션이 동시에 획득할 수 있는 읽기 잠금이고, 배타 잠금은 변경을 위해 획득하며 다른 트랜잭션의 읽기·쓰기 잠금과 충돌한다.

InnoDB의 일반 SELECT는 MVCC를 이용해 잠금 없이 읽을 수 있다. 명시적인 잠금 읽기에는 FOR SHARE와 FOR UPDATE를 사용한다.

### 10.2 데드락

데드락은 트랜잭션들이 서로가 가진 잠금이 풀리기를 기다리는 상태다. 여러 테이블과 행을 항상 같은 순서로 잠그고, 트랜잭션을 짧게 유지하며, 발생 시 한 트랜잭션을 롤백하고 재시도하는 방식으로 대응한다.

### 10.3 직렬화

직렬화는 객체를 네트워크 전송이나 저장이 가능한 형태로 변환하는 과정이다.

- HTTP API: DTO를 JSON으로 변환
- Redis: 객체를 문자열이나 JSON으로 변환
- Kafka: 이벤트를 byte 데이터로 변환
- 역직렬화: 전송·저장된 데이터를 다시 객체로 변환

Java 기본 직렬화는 외부 API나 장기 저장 포맷으로 사용하기보다, API와 이벤트에 JSON, Avro, Protobuf처럼 형식과 스키마가 명확한 방식을 사용하는 것이 적합하다.

### 10.4 외래 키

외래 키는 자식 테이블의 값이 부모 테이블에 실제로 존재하는지 확인하는 제약 조건이다.

- RESTRICT: 참조 중인 부모 삭제 금지
- CASCADE: 부모 삭제 시 자식도 삭제
- SET NULL: 부모 삭제 시 외래 키를 NULL로 변경
- NO ACTION: MySQL InnoDB에서는 사실상 RESTRICT처럼 동작

회원 삭제 시 채팅과 거래 기록까지 함께 삭제되는 것이 서비스 정책에 맞는지 확인한 뒤 CASCADE를 사용한다.