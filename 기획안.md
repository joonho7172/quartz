git status --short --branch
git switch -c backend/auth

이미 잇다면 
git switch backend/auth

git add -A apps/backend
git diff --cached --check
git diff --cached --stat

커밋
git commit -m "feat: implement backend authentication"

푸시
git push -u origin backend/auth

pr생성 
gh pr create --base main --head backend/auth --title "feat: implement backend authentication" --body "회원가입, 로그인, 토큰 갱신, 로그아웃 및 JWT 인증 구현"