
1. 로그인 버튼 클릭
    
    `GET /auth/oauth/state` 호출 후 응답의 `state`를 저장합니다.  
    이때 `oauth_state` 쿠키도 함께 저장되므로 요청에 `credentials: include`가 필요합니다.
    
2. Kakao 로그인 페이지 이동
    
    Kakao authorize URL에 다음 값을 포함합니다.
    
    - REST API 키
    - redirect URI
    - `response_type=code`
    - 발급받은 `state`
3. Callback 페이지 구현
    
    프론트엔드의 callback 페이지에서 URL의 다음 값을 읽습니다.
    
    - `code`
    - `state`
    
    저장해 둔 state와 callback의 state가 같은지 확인합니다.
    
4. 백엔드 로그인 요청
    
    `POST /auth/oauth` 호출:
    
    ```
    {
      "provider": "KAKAO",
      "authorizationCode": "code",
      "state": "state"
    }
    ```
    
    쿠키 전송을 위해 `credentials: include`를 사용합니다.
    
5. Access Token 사용
    
    로그인 응답의 `accessToken`을 저장하고 API 요청마다 다음 헤더를 붙입니다.
    
    ```
    Authorization: Bearer <accessToken>
    ```
    
6. 토큰 만료 처리
    
    API가 `401`을 반환하면 `POST /auth/refresh`를 호출합니다.  
    새 access token을 받은 뒤 원래 요청을 한 번 재시도합니다.
    
7. 로그아웃
    
    `POST /auth/logout` 호출 후 프론트의 access token을 삭제하고 로그인 화면으로 이동합니다.
    

프론트엔드가 가지면 안 되는 값:

- JWT secret
- Kakao client secret
- Kakao Admin key

현재 백엔드에는 `/auth/kakao/callback` 컨트롤러가 없으므로, 프론트엔드가 callback 페이지를 구현해야 합니다. 이 경우 Kakao 콘솔의 redirect URI와 백엔드의 `KAKAO_REDIRECT_URI`를 프론트 callback 주소로 동일하게 맞춰야 합니다.

또한 프론트와 백엔드가 다른 포트라면 CORS 설정과 쿠키 전송 설정이 필요합니다.