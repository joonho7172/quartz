## 1. 로그인 시작: state 발급

프론트엔드가 로그인 버튼을 누르면 먼저 요청합니다.

```
GET http://localhost:8080/auth/oauth/state
```

쿠키를 유지해야 하므로 프론트에서는 다음처럼 호출합니다.

```
const response = await fetch("http://localhost:8080/auth/oauth/state", {
  credentials: "include"
});

const { state } = (await response.json()).data;
```

백엔드 응답:

```
HTTP/1.1 200 OK
Set-Cookie: oauth_state=<state>; HttpOnly; Secure; SameSite=Lax; Path=/auth; Max-Age=300
```

```
{
  "data": {
    "state": "<state>",
    "expiresIn": 300
  },
  "error": null
}
```

백엔드는 원본 state가 아니라 SHA-256 해시를 저장하고, 5분 동안 한 번만 사용할 수 있게 합니다.

## 2. 프론트에서 Kakao 로그인 페이지로 이동

프론트는 응답받은 `state`를 Kakao 로그인 URL에 넣어 이동시킵니다.

```
https://kauth.kakao.com/oauth/authorize
  ?client_id=<KAKAO_CLIENT_ID>
  &redirect_uri=http://localhost:8080/auth/kakao/callback
  &response_type=code
  &state=<state>
```

Kakao 로그인 성공 후 Kakao가 프론트가 아니라 백엔드 callback으로 이동합니다.

```
GET /auth/kakao/callback?code=<authorizationCode>&state=<state>
Cookie: oauth_state=<same-state>
```

## 3. 백엔드의 Kakao 인증 처리

백엔드는 내부적으로 다음 작업을 합니다.

1. 쿠키의 `oauth_state`와 query parameter의 `state` 비교
2. state가 유효하면 일회성 소비
3. Kakao 토큰 API 호출

```
POST https://kauth.kakao.com/oauth/token
Content-Type: application/x-www-form-urlencoded
```

4. Kakao 사용자 정보 API 호출

```
GET https://kapi.kakao.com/v2/user/me
Authorization: Bearer <Kakao access token>
```

5. Kakao 사용자 ID로 기존 `SocialAccount` 조회
6. 최초 로그인이라면 `User`와 `SocialAccount` 생성
7. 기존 사용자라면 로그인 시간 갱신
8. 우리 서비스용 Access Token과 Refresh Token 발급

Kakao Access Token은 프론트로 전달하지 않고 백엔드 내부에서만 사용합니다.

## 4. callback 응답

로그인 성공 시 callback은 Access Token을 응답 body에 넣지 않습니다.

```
HTTP/1.1 302 Found
Location: http://localhost:3000
Set-Cookie: refresh_token=<refreshToken>;
            HttpOnly;
            Secure;
            SameSite=Lax;
            Path=/auth;
            Max-Age=1209600
```

즉, 브라우저는 프론트엔드 주소로 이동하고 Refresh Token은 HttpOnly 쿠키로 저장됩니다.

## 5. 프론트가 Access Token 요청

프론트엔드 페이지가 로드되면 `/auth/refresh`를 호출합니다.

```
POST http://localhost:8080/auth/refresh
Cookie: refresh_token=<refreshToken>
```

프론트 코드 예시:

```
const response = await fetch("http://localhost:8080/auth/refresh", {
  method: "POST",
  credentials: "include"
});

const { accessToken } = (await response.json()).data;
```

백엔드 응답:

```
{
  "data": {
    "accessToken": "<our-access-token>",
    "tokenType": "Bearer",
    "expiresIn": 900
  },
  "error": null
}
```

Access Token 기본 만료 시간은 15분입니다. Refresh Token은 기본 14일이며, `/auth/refresh`에서는 Refresh Token을 새로 교체하지 않고 Access Token만 재발급합니다.

## 6. 로그인 이후 보호된 API 호출

프론트는 Access Token을 다음처럼 헤더에 넣습니다.

```
Authorization: Bearer <accessToken>
```

예시:

```
fetch("http://localhost:8080/users", {
  headers: {
    Authorization: `Bearer ${accessToken}`
  }
});
```

백엔드는 JWT의 서명, 만료 시간, `sub`, `role`을 검증합니다.

- `sub`: 내부 사용자 ID
- `role`: `USER` 또는 `ADMIN`
- 알고리즘: HS256

검증에 성공하면 `SecurityContext`에 로그인 사용자 정보를 저장합니다.

## 7. Access Token 만료 시

프론트가 보호된 API에서 `401`을 받으면 다음 순서로 처리하면 됩니다.

```
1. POST /auth/refresh
2. 새 Access Token 저장
3. 기존 API 재요청
4. refresh도 401이면 로그인 화면으로 이동
```

Refresh Token이 없거나 만료·폐기되었으면 다음 응답을 받습니다.

```
{
  "data": null,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Refresh Token이 만료되었거나 유효하지 않습니다.",
    "details": []
  }
}
```

## 8. 로그아웃

로그아웃은 Access Token과 Refresh Token 쿠키가 모두 필요합니다.

```
POST /auth/logout
Authorization: Bearer <accessToken>
Cookie: refresh_token=<refreshToken>
```

응답:

```
HTTP/1.1 204 No Content
Set-Cookie: refresh_token=; Max-Age=0
```

백엔드는 DB의 Refresh Token을 폐기하고 브라우저 쿠키도 삭제합니다.

## 직접 로그인 요청 방식

Kakao callback을 프론트에서 직접 받는 구조라면 프론트가 다음 요청을 보낼 수도 있습니다.

```
POST /auth/oauth
Content-Type: application/json
Cookie: oauth_state=<state>
```

```
{
  "provider": "KAKAO",
  "authorizationCode": "<authorizationCode>",
  "state": "<state>"
}
```

신규 사용자:

```
201 Created
```

기존 사용자:

```
200 OK
```

응답 body:

```
{
  "data": {
    "accessToken": "<accessToken>",
    "tokenType": "Bearer",
    "expiresIn": 900,
    "isNewUser": true,
    "user": {
      "userId": 1,
      "nickname": "사용자",
      "profileImageUrl": "https://..."
    }
  },
  "error": null
}
```

이 방식에서는 Access Token이 바로 JSON으로 반환되므로 callback 방식처럼 즉시 `/auth/refresh`를 호출할 필요는 없습니다.

## 프론트 연동 시 주의점

- Refresh Token은 `HttpOnly`라서 프론트 JavaScript가 읽을 수 없습니다.
- `/auth/oauth/state`, `/auth/refresh`, `/auth/logout` 요청에는 `credentials: "include"`가 필요합니다.
- 프론트가 `localhost:3000`, 백엔드가 `localhost:8080`이면 CORS에서 credential 허용이 필요합니다.
- `localhost`와 `127.0.0.1`을 섞으면 쿠키가 다르게 저장될 수 있습니다.
- 현재 쿠키의 Path는 `/api/auth`가 아니라 `/auth`입니다.