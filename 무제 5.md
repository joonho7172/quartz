주요 변경:

- [ErrorCode.java (line 5)](/Users/kim/Desktop/KTB-Agile-backend/src/main/java/com/example/KTB_Agile_backend/common/exception/ErrorCode.java:5)
    
    - `AUTH_REFRESH_TOKEN_INVALID`, `AUTH_PROVIDER_UNSUPPORTED`처럼 도메인 의미가 있는 코드 추가
    - 기본 메시지를 `ErrorCode`가 관리
    - 별도 `ErrorMessage` 클래스는 만들지 않음
- [ApiException.java (line 7)](/Users/kim/Desktop/KTB-Agile-backend/src/main/java/com/example/KTB_Agile_backend/common/exception/ApiException.java:7)
    
    - `ErrorResponse` 의존성 제거
    - 예외는 `ErrorCode`, 메시지, 상세 정보, 원인 예외만 보관
    - 응답 DTO 생성은 Handler로 이동
- [GlobalExceptionHandler.java (line 22)](/Users/kim/Desktop/KTB-Agile-backend/src/main/java/com/example/KTB_Agile_backend/common/exception/GlobalExceptionHandler.java:22)
    
    - Auth URI 기반 `switch` 제거
    - 예상하지 못한 오류는 모든 도메인에서 동일한 500 응답 반환
    - `authorizationCode` 등 Auth 전용 검증 로직 제거
- [SecurityConfig.java (line 81)](/Users/kim/Desktop/KTB-Agile-backend/src/main/java/com/example/KTB_Agile_backend/security/SecurityConfig.java:81)
    
    - `/auth/logout` 여부에 따른 401 메시지 분기 제거
    - `ErrorCode`의 기본 메시지 사용
- `AuthService`, `OAuthStateService`, `RefreshTokenService`, `KakaoOAuthProviderClient`, `AccountProvisioningService`, `UserPreferenceService`
    
    - 기존의 generic `BAD_REQUEST`, `UNAUTHORIZED`를 도메인별 오류 코드로 변경
- [GlobalExceptionHandlerTest.java (line 13)](/Users/kim/Desktop/KTB-Agile-backend/src/test/java/com/example/KTB_Agile_backend/common/exception/GlobalExceptionHandlerTest.java:13) 추가
    
    - `/auth/refresh`, `/groups`처럼 URI가 달라도 동일한 500 응답을 반환하는지 검증

검증 결과:

```
./gradlew test
BUILD SUCCESSFUL
```

면접에서는 이렇게 설명하면 됩니다.

> 기존에는 `GlobalExceptionHandler`가 요청 URI를 보고 Auth 전용 오류 메시지를 선택하고 있어, 공통 예외 처리 계층이 특정 도메인에 결합되어 있었습니다. 이를 도메인 의미를 가진 `ErrorCode` 중심 구조로 개선했습니다. 예외 객체는 오류 코드와 상세 정보만 보관하고, 실제 HTTP 응답 변환은 `GlobalExceptionHandler`가 담당하도록 책임을 분리했습니다. 예상하지 못한 서버 오류는 모든 도메인에서 동일한 일반 메시지를 반환하며, Security Filter 단계의 401·403 오류도 같은 오류 계약을 사용하도록 통일했습니다. 따라서 새로운 도메인이 추가되어도 Global Handler를 수정하지 않고 오류 코드와 서비스의 예외 발생 지점만 추가하면 됩니다.