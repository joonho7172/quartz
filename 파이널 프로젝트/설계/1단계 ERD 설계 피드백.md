1. view count 바뀔 때마다 테이블 갱신 될 텐데 성능적으로 괜찮을지
	1. view count를 세는 것을 따로 둘까
			이거는 고민 좀 해봐야 할듯
	
2. user _ preference 라는 이름 이상하다. 뭔가 초기 설정 값 같은데 성향은 다른 이름 고려
	1. trade_profile로 변경

3. 카카오 인증 밖에 없는데 다른 로그인이 추가 될 때마다 컬럼을 추가하는게 말이 안됨..
	1. 따로 빼서 분리햇다. social login으로,,

4. Group과 Group request items 에서 복합키 설정에 대해서 이해하기 JPA랑 연결지어서 이해를 할 것
	1. JPA는 기본적으로 서로키, 하나의 pk를 두는 것을 목적으로 만들어진 프레임워크임. 그래서 복합키를 두면 EmbeddedId나, @IdClass등을 두어야 함. 그리고 매번 식별자를 묶어서 검색하는 검색 객체도 만들어줘야하고, 이 테이블을 또 누군가가 참조를 하게 되면 복합키 전체를 FK로 들고 가야함. JPQL도 2개 묶어서 다뤄야 하고..

5. Exchange request에서 중복된 연관관계 찾기.. item이랑 user가 엮어져 있는데 하나만 엮어서 join타고 넘어가면 되지 않나..
	1. 흠.. 근데 item은 교환 대상의 item이고 user는 교환 한 사람의 user라서 

6. JPA에서 creted_at같은 경우는 공통 entity로 두고 관리하기도 함, 없는 것들은 생각 해볼것.
	넵 생각해보겟습니다
	
7. deleted에 대해서는 soft deleted로 처리하고 통일 시킬 것.
		넵
		
8. varchar 쓰는 이유 왜 enum 안쓰나요
		mysql과 postgresql에서는 enum을 지원하지만 my sql 서버와 다른 곳에는 지원하지 않아서 마이그레이션에 불합리함이 있을 수 있다.

		무엇보다 유연한 스키마 변경이 안된다. enum 같은 경우는 String 같지만 내부적으로는 숫자를 쓰기 때문에 , 속성을 삭제해버리면 기존에 그 값을 쓰던 데이터 행이 ALTER가 불가능해지거나 값이 깨질 수 있다. 결국 테이블 구조 변경에 enum은 너무 많은 비용이 든다. erd는 개발, 운영 과정에서 자주 바뀔 수 있는데도..
		
9. 위치 인증 유효 데이터 Null?
		not null로 바꿧습니다
10. 이미지 테이블 한번에 통일 ㄱㄱ

	넵
11. 신고 대상자랑 신고 물품 둘 다 외래키로 필요한지..
		넵 신고 대상자는 뺏습니다.
		
12. report 랑 reportaction 1:1인 이유는?

13. ai기능 때문애 테이블 분리는 말이 안되는 것
	1. 일단 인지
14. create at두는 이유는 데이터 정합이전에 국룰 처럼 씀 컨벤션 처럼
	1. create at 공통 entity로 두는 것 생각해볼것.
	   
	   
	   
	   공부
13. text쓰면 검색 속도 느려지는지 알아보기ㅣ, varchar , float 